

            for (int i = 0; i < n; i++)
            {
                if (i == n - 1)
                    v.push_back({i, 1});
                else
                {
                    v.push_back({i, i + 1});
                }
            }
        